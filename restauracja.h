#ifndef RESTAURACJA_H
#define RESTAURACJA_H

#include "procesy.h"
#include <errno.h>

// ====== MAIN PROGRAM HEADER ======

/**
 * Main function of the restaurant simulation
 * Creates and manages all processes (service, cook, manager, client generator)
 * @return 0 on success, 1 on error
 */
int main(void);

#endif // RESTAURACJA_H
